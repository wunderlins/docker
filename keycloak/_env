# copy this file to .env, adjust to your liking 
#
# then run:
# $ docker compose build
# $ docker compose up -d

# https://quay.io/repository/keycloak/keycloak?tab=tags&tag=latest
IMAGE_VERSION=latest
#IMAGE_VERSION=19.0

# must use FQDN in production for encryption to work properly
HOSTNAME=localhost

# Application config
KEYCLOAK_LISTEN_PORT=8443
KEYCLOAK_ADMIN=admin1
KEYCLOAK_ADMIN_PASSWORD=password1